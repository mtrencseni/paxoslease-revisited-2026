SPECIFICATION Spec
CONSTANTS
    Proposers = {p1, p2}
    Acceptors = {a1, a2}
    Ballots = {1, 2}
    NoOwner = NoOwner
    MaxTime = 3
    ProposerDuration = 1
    AcceptorDuration = 2
    RetryTimeout = 2
    TimelyDispatch = TRUE
    Quarantine = 2
    CrashableAcceptors = {a1, a2}
    AllowCrash = TRUE
    AllowRelease = FALSE
    AllowDrop = FALSE
    AllowRenewal = FALSE
    CrashDropsIncoming = FALSE
    MaxNetwork = 4
INVARIANT TypeOK
INVARIANT Safety
CONSTRAINT StateConstraint
INVARIANT AttemptFreshness
